# HAMS Frontend (Create React App + Tailwind CSS)
Quick start:
1. Install dependencies: `npm install`
2. Run development server: `npm start`
3. Default API base URL: http://localhost:8080/api (change in src/api/api.js)
